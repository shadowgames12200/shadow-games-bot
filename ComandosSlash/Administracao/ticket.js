const { SlashCommandBuilder, PermissionFlagsBits } = require('discord.js');
const { panel, adminPanel } = require('../../TicketControlSuiteEnhanced');
const data = new SlashCommandBuilder().setName('ticket').setDescription('Sistema único de suporte, dúvidas, compras e atendimento').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild.toString()).addSubcommand(command => command.setName('painel').setDescription('Publica o painel público de atendimento')).addSubcommand(command => command.setName('configurar').setDescription('Abre o painel administrativo de tickets'));
module.exports = { name: 'ticket', data, run: async (_client, interaction) => { if (!interaction.memberPermissions?.has(PermissionFlagsBits.ManageGuild) && !interaction.memberPermissions?.has(PermissionFlagsBits.Administrator)) return interaction.reply({ content: '❌ Você precisa de Gerenciar Servidor para usar este comando.', ephemeral: true }); return interaction.reply((interaction.options?.getSubcommand?.(false) === 'configurar') ? adminPanel() : panel()); } };
